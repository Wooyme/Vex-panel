# MuJoCo Arcade Robot Control Panel

A browser-based robot control panel with MuJoCo/URDF visualization, virtual joysticks,
height adjustment, configurable function buttons, and MQTT-over-WebSocket messaging.

## Run Locally

**Prerequisites:** Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
